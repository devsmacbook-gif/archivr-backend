[build]
builder = "nixpacks"

[deploy]
startCommand = "python -m playwright install chromium && python -m playwright install-deps chromium && uvicorn main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
