[build]
builder = "nixpacks"
buildCommand = "pip3 install -r requirements.txt && python3 -m playwright install chromium --with-deps"

[deploy]
startCommand = "uvicorn main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
